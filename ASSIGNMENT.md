# Assignment 1 — React SPA: Pokédex Team Builder

**15% ของคะแนนรวม · ทำเป็นคู่ (กลุ่มเดียวกับ Final Project) · 📣 แจก เสาร์ 19 ก.ย. 69 (วันที่ 5) · 📤 ส่งภายใน เสาร์ 26 ก.ย. 69 เวลา 09:00 น. (ก่อนเข้าคาบวันที่ 7)**

> 📄 **ไฟล์นี้คือโจทย์ฉบับนักศึกษา** — อ่านให้จบก่อนเริ่มเขียนโค้ด · ถ้าโจทย์ไม่ชัดตรงไหน ถาม TA/อาจารย์ในกลุ่มคลาส อย่าเดาเอง

> **ครอบเนื้อหาวันที่ 1–5:** component/props · `useState` + controlled input · list/`key` · `useEffect` + custom hook · React Router (layout route, `useParams`, `useSearchParams`, 404) · **Context + custom hook** · **react-hook-form + zod** · decision framework ของ state
> 💡 **ต่อยอดได้จากของที่มีแล้ว:** โครง route/`useFetch` เหมือน Recipe Browser วันที่ 4 · Context + `useX()` เหมือน `useCart()` วันที่ 5 · ฟอร์มเหมือน Checkout วันที่ 5
> ⚠️ **มี Proposal Final Project ส่งวันที่ 6 คั่นกลาง** — อย่าทิ้งไว้ทำคืนวันศุกร์
> 🔓 **ใช้ AI ช่วยเขียนได้** แต่ **ต้องอธิบายทุกบรรทัดในรีโปได้**
> 🔎 **ต้องตอบ README ข้อ 3 ให้ได้ด้วยตัวเอง** — เป็นที่เดียวที่วัดว่าเข้าใจโค้ดที่ส่ง (มีคะแนนในแถว 8 ของ rubric)
> ❌ ไม่ต้อง deploy (สอนวันที่ 8 พร้อม Next.js) · ❌ ไม่มีนำเสนอหน้าห้อง · ❌ ห้ามใช้ Redux/Zustand (โจทย์นี้ Context พอ — ใช้แล้วหักแถว 5)

---

## 1. โจทย์

สร้าง Single Page Application ด้วย **Vite + React 19 + React Router v7 + Tailwind CSS + react-hook-form + zod** สำหรับเรียกดู Pokémon รุ่นแรก (**#1–#151 เท่านั้น**) จัด "ทีมของฉัน" ได้สูงสุด 6 ตัว และลงทะเบียนทีมผ่านฟอร์ม

> 🖼 **หน้าตาเป้าหมาย:** เปิด `mockup-pokedex.html` ในโครงตั้งต้นด้วยเบราว์เซอร์ — เป็นภาพอ้างอิงเท่านั้น **rubric ไม่มีแถวคะแนนความสวย** จัดหน้าเป็นแบบอื่นได้ ขอแค่ข้อมูลและปุ่มครบตามโจทย์

**API:** [PokéAPI](https://pokeapi.co/) (ฟรี ไม่ต้องใช้ key)

| ใช้ทำอะไร                                | Endpoint                                                                                                                     |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| รายชื่อทั้ง 151 ตัว (ยิงครั้งเดียว)      | `https://pokeapi.co/api/v2/pokemon?limit=151` → `results: [{ name, url }]` · id คือตัวเลขท้าย `url`                          |
| รายละเอียด 1 ตัว (รับได้ทั้งชื่อหรือ id) | `https://pokeapi.co/api/v2/pokemon/{nameOrId}`                                                                               |
| Pokémon ตามธาตุ                          | `https://pokeapi.co/api/v2/type/{type}` → `pokemon: [{ pokemon: { name, url } }]` (**มีทุกรุ่น ต้องกรองเหลือ id ≤ 151 เอง**) |
| รูป (ไม่ต้อง fetch)                      | `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/{id}.png`                   |

> ⚠️ PokéAPI **คืน HTTP 404** เมื่อไม่พบชื่อ/id (ต่างจาก TheMealDB ของวันที่ 4 ที่คืน `meals: null` แบบ HTTP 200)

---

## 2. Routes ที่ต้องมี

| Route                | หน้า       | ต้องมีอะไร                                                                                                                      |
| -------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `/`                  | หน้าแรก    | ข้อความต้อนรับ + ลิงก์ไป `/pokemon` + จำนวนสมาชิกทีมตอนนี้                                                                      |
| `/pokemon`           | รายการ     | การ์ด (รูป + `#id` + ชื่อ + ป้าย "อยู่ในทีม" ถ้าอยู่) · ค้นหาด้วยชื่อ · กรองตามธาตุ · loading / error / ไม่พบผลลัพธ์            |
| `/pokemon/:nameOrId` | รายละเอียด | รูป · ชื่อ · `#id` · ธาตุ · ส่วนสูง (**เมตร**) · น้ำหนัก (**กิโลกรัม**) · ปุ่ม เพิ่มเข้าทีม/เอาออกจากทีม · ลิงก์ ก่อนหน้า/ถัดไป |
| `/team`              | ทีมของฉัน  | การ์ดสมาชิก (0–6 ตัว) · ปุ่มเอาออกทีละตัว · ปุ่มล้างทีม · ข้อความเมื่อทีมว่าง · **ฟอร์มลงทะเบียนทีม** (R6)                      |
| `*`                  | 404        | ข้อความ + ปุ่มกลับหน้าแรก                                                                                                       |

- [ ] ทุกหน้าอยู่ใต้ **layout route** เดียว (`Nav` + `<Outlet />` + footer)
- [ ] ลิงก์ภายในแอปใช้ `Link`/`NavLink` เท่านั้น · `NavLink` มี active state ถูกต้อง
- [ ] `Nav` แสดงจำนวนสมาชิกทีม เช่น `ทีม (3/6)`

---

## 3. ความต้องการ (Requirements)

### R1 — Data fetching

- [ ] ใช้ **custom hook `useFetch(url)`** กับทุกจุดที่ fetch — ห้ามเขียน `fetch` + `useEffect` ซ้ำในไฟล์หน้า
- [ ] หน้า `/pokemon` และหน้ารายละเอียดมีครบ **3 สถานะ**: loading / error / ไม่พบ
- [ ] `useFetch` เช็ก `res.ok` และมี cleanup กัน response เก่ามาทับ response ใหม่
- [ ] `useFetch` ต้องรองรับ **`url` เป็น `null` = ไม่ยิง** — หน้ารายการเรียก hook ไว้เสมอ แต่ยิง endpoint ธาตุเฉพาะตอนที่เลือกธาตุจริง ๆ (ของวันที่ 3–4 ยังไม่รองรับ ต้องเติมเอง)

### R2 — คำค้นและธาตุอยู่ใน URL

- [ ] คำค้นและธาตุเก็บใน query string (`/pokemon?q=char&type=fire`) ด้วย `useSearchParams` — **ห้ามเก็บซ้ำใน `useState`**
- [ ] ค้นหาแบบ **ไม่สนตัวพิมพ์ใหญ่เล็ก** และจับ **บางส่วน** ของชื่อ (`CHAR` เจอ charmander, charmeleon, charizard)
- [ ] ค้นหา + ธาตุ **ทำงานพร้อมกัน** (AND) · กรองจากรายชื่อที่ fetch มาแล้ว — **ไม่ยิง API ใหม่ทุกครั้งที่พิมพ์**
- [ ] dropdown ธาตุมีอย่างน้อย: ทั้งหมด, normal, fire, water, grass, electric, psychic, poison, bug, rock, ghost, dragon

### R3 — หน้ารายละเอียด

- [ ] อ่าน `nameOrId` ด้วย `useParams` อย่างเดียว — เปิด URL ตรง ๆ ต้องทำงานเหมือนกดลิงก์มา
- [ ] แปลงหน่วย: API ให้ `height` เป็น **เดซิเมตร** และ `weight` เป็น **เฮกโตกรัม** → แสดงเป็น ม. และ กก.
- [ ] ลิงก์ ก่อนหน้า/ถัดไป ใช้ id (`#1` ไม่มีปุ่มก่อนหน้า · `#151` ไม่มีปุ่มถัดไป)

### R4 — ทีมของฉันด้วย Context

- [ ] `src/context/TeamContext.jsx` มี `TeamProvider` + custom hook **`useTeam()`** — ทั้งโปรเจกต์เรียก `useContext(TeamContext)` ได้ **ที่เดียว** คือใน `useTeam()`
- [ ] `useTeam()` เรียกนอก `TeamProvider` ต้อง throw error ภาษาคน (ไม่ใช่ `Cannot read properties of null`)
- [ ] **สูงสุด 6 ตัว** · **ห้ามซ้ำ** · ทีมเต็มแล้วปุ่มเพิ่มต้อง disabled พร้อมบอกเหตุผล
- [ ] ค่าที่คำนวณได้ (เช่น `count`, `isFull`, `has(id)`) **คำนวณตอน render** ไม่เก็บเป็น state แยก
- [ ] อัปเดต state แบบ immutable

### R5 — ทีมอยู่รอดหลัง refresh

- [ ] เก็บทีมใน `localStorage` · refresh หน้าแล้ว **ทีมยังอยู่ครบ**
- [ ] ข้อมูลใน `localStorage` เสีย/ไม่ใช่รูปแบบที่คาด → แอป **ต้องไม่จอขาว** (ถือว่าทีมว่าง)

### R6 — ฟอร์มลงทะเบียนทีม (react-hook-form + zod)

อยู่ในหน้า `/team` · schema อยู่ในไฟล์แยก `src/schemas/registration.js`

| ช่อง          | กติกา                                                                                                       |
| ------------- | ----------------------------------------------------------------------------------------------------------- |
| ชื่อเทรนเนอร์ | 2–30 ตัวอักษร                                                                                               |
| ชื่อทีม       | 3–20 ตัวอักษร · ใช้ได้เฉพาะ A–Z a–z 0–9 และช่องว่าง · **ห้ามซ้ำกับชื่อ Pokémon ตัวใดในทีม (ไม่สนตัวพิมพ์)** |
| อีเมล         | รูปแบบอีเมลถูกต้อง                                                                                          |
| ยืนยันอีเมล   | ต้องตรงกับอีเมล (error ขึ้นที่ช่องนี้ ไม่ใช่ระดับฟอร์ม)                                                     |

- [ ] ต่อด้วย `zodResolver` · error ขึ้นใต้ช่องที่ผิด · ใช้ `mode` ที่ไม่ขึ้น error ก่อนผู้ใช้เริ่มกรอก
- [ ] **ทีมต้องมีอย่างน้อย 3 ตัว** ถึงจะส่งได้ — ถ้าน้อยกว่า ปุ่มส่ง disabled พร้อมบอกว่าขาดอีกกี่ตัว
- [ ] กดส่ง → จำลองรอ 800 ms (ปุ่ม disabled + ข้อความ "กำลังส่ง…") → แสดงการ์ดสรุป: ชื่อเทรนเนอร์ ชื่อทีม และรายชื่อสมาชิก · **ไม่ต้องมี backend**

### R7 — คุณภาพโค้ด

- [ ] มี component `PokemonCard` ใช้ซ้ำทั้งหน้ารายการและหน้าทีม
- [ ] ทุก `.map()` มี `key` ที่เสถียร (ห้าม index)
- [ ] `npm run build` ผ่านไม่มี error

---

## 4. สิ่งที่ต้องส่ง

ส่งโดย **อัปโหลดไฟล์ `.zip` ของโปรเจกต์ลงใน Google Sheet ของรายวิชา** **[TBD — ลิงก์ Sheet]** (ช่องทางเดียวกับที่ใช้ส่งงานแล็บ) — หนึ่งแถวต่อหนึ่งกลุ่ม: ชื่อกลุ่ม · ชื่อ-รหัสนักศึกษาทั้งสองคน · ไฟล์งาน

- [ ] 🔴 **ชื่อไฟล์: `assignment-01-<ชื่อกลุ่ม>.zip`**
- [ ] 🔴 **ลบโฟลเดอร์ `node_modules` ออกก่อน zip** — ไม่ลบไฟล์จะใหญ่หลายร้อย MB อัปโหลดไม่ขึ้น
- [ ] 🔴 **แตก zip แล้วต้องเจอ `package.json` อยู่ชั้นบนสุด** ไม่ใช่ซ้อนโฟลเดอร์อีกชั้น
- [ ] เริ่มจากโครงตั้งต้น `assignment-01-start` ที่อาจารย์แจก (มีแพ็กเกจครบ + `src/lib/pokemon.js` (helper ที่ให้มา ไม่มีคะแนน) + `README.md` แบบฟอร์ม 3 หัวข้อ + `mockup-pokedex.html`) · ไฟล์ที่ต้องเขียนทุกไฟล์มีคอมเมนต์บอกว่าเป็นข้อไหนของโจทย์
- [ ] โค้ดที่ `npm install && npm run dev` รันได้ทันที
- [ ] `README.md` ต้องมีครบ 3 หัวข้อ:
  1. **สมาชิก** — ชื่อ-นามสกุล · รหัสนักศึกษา
  2. **วิธีรัน**
  3. **State อยู่ที่ไหน ทำไม** — ตอบ 3 ข้อ: (ก) ทำไมคำค้น/ธาตุอยู่ใน URL ไม่ใช่ `useState` — ผู้ใช้ได้อะไร (ข) ไล่ decision framework ของวันที่ 5 (`useState` → lift → Context → Redux/Zustand) ว่าทำไมทีมหยุดที่ Context (ค) ใช้ `memo`/`useMemo`/`useCallback` ที่ไหนบ้าง ทำไม — **หรือตอบว่าไม่ได้ใช้เลยก็ได้เต็ม** ขอแค่บอกได้ว่าทำไมแอปขนาดนี้ยังไม่จำเป็น

**เส้นตายวัดจากเวลาที่อัปโหลดไฟล์ลง Sheet** — ไฟล์ที่อัปโหลดหลัง 09:00 น. วันที่ 26 ก.ย. 69 นับเป็นส่งช้า · อัปโหลดทับได้เรื่อย ๆ ก่อนหมดเวลา TA ตรวจไฟล์ล่าสุด

**ส่งช้า:** หักวันละ 10% ของคะแนน Assignment นี้ · **เกิน 3 วันไม่รับ** (หลัง 09:00 น. อังคาร 29 ก.ย. 69) (ตามนโยบายส่งงานล่าช้าของรายวิชา) — ถ้าจะส่งช้าให้แจ้ง TA ก่อน แล้วนับวันช้าจากเวลาที่อัปโหลดไฟล์สุดท้าย

---

## 5. Twist — จุดที่แอปมักพังโดยที่ไม่รู้ตัว

แอปที่ "ดูใช้งานได้" ตอนกดเล่นปกติ ยังพังได้ตรงนี้ — **ทุกข้อในนี้ต้องผ่าน**

1. 🔴 เปิด `/pokemon?q=saur&type=grass` **ในแท็บใหม่** → ช่องค้นหามี `saur` · dropdown เป็น grass · ผลลัพธ์ตรงทันที
2. 🔴 พิมพ์คำค้น 5 ตัวอักษรแล้วกด back ของเบราว์เซอร์ **ครั้งเดียว** → ต้องออกจากการค้นหา ไม่ใช่ลบทีละตัวอักษร
3. 🔴 เปิด `/pokemon/152` หรือ `/pokemon/chikorita` (**มีจริงใน API แต่อยู่นอก 151 ตัว**) → ต้องขึ้นว่าไม่อยู่ใน Pokédex นี้ · และเลือกธาตุ fire ต้องไม่มี #155 cyndaquil
4. 🔴 เปิด `/pokemon/notapokemon` → ข้อความ "ไม่พบ" ไม่ใช่จอขาวหรือ loading ค้าง · เปิด `/pokemon/25` กับ `/pokemon/pikachu` → ได้หน้าเดียวกัน
5. 🔴 ในหน้ารายละเอียด กด "ถัดไป" รัว ๆ 10 ครั้ง → ข้อมูลที่ค้างสุดท้ายต้องตรงกับ URL
6. 🔴 เพิ่มตัวที่ 7 ไม่ได้ · เพิ่มตัวซ้ำไม่ได้ · ตัวเลขใน `Nav` และป้าย "อยู่ในทีม" เปลี่ยนทันทีไม่ว่ากดจากหน้าไหน · refresh แล้วทีมยังอยู่
7. 🔴 แก้ค่าทีมใน `localStorage` ด้วยมือเป็น `abc` แล้ว refresh → ไม่จอขาว ทีมว่าง
8. 🔴 ทีมมี pikachu แล้วตั้งชื่อทีมว่า `Pikachu` → error ที่ช่องชื่อทีม · ลบ pikachu ออกจากทีมแล้วกดส่งอีกครั้ง → ผ่าน
9. 🔴 ทีมมี 2 ตัว → ส่งไม่ได้ และบอกว่าขาดอีก 1 ตัว · เปิดหน้า `/team` ครั้งแรก → ยังไม่มี error แดงขึ้นก่อนแตะช่องใด ๆ

---

## 6. Rubric (100 คะแนน → คิดเป็น 15% ของคะแนนรวม)

|  #  | เกณฑ์                            |  คะแนน  | ได้เต็มเมื่อ                                                                                                                                                    |
| :-: | -------------------------------- | :-----: | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|  1  | **Routing & Layout**             |   10    | ครบ 5 route · layout route เดียว · `Link`/`NavLink` ทั้งหมด (ไม่มี full reload) · active state ถูก · 404 ทำงาน                                                  |
|  2  | **Data fetching**                |   15    | `useFetch` ใช้ทุกจุด · 3 สถานะครบ · เช็ก `res.ok` · cleanup ถูก · Twist 4, 5 ผ่าน                                                                               |
|  3  | **ค้นหา/กรองใน URL**             |   15    | `useSearchParams` ไม่มี `useState` ซ้ำ · ค้นหา + ธาตุทำงานร่วมกัน · กรองจากข้อมูลที่มี · Twist 1, 2 ผ่าน                                                        |
|  4  | **หน้ารายละเอียด**               |   10    | ข้อมูลครบ · แปลงหน่วยถูก · ก่อนหน้า/ถัดไปถูกที่ขอบ · Twist 3 ผ่าน                                                                                               |
|  5  | **ทีม: Context + localStorage**  |   20    | `TeamProvider` + `useTeam()` เป็นทางเข้าเดียว · guard error · กฎ 6 ตัว/ห้ามซ้ำ · ค่าคำนวณไม่เก็บเป็น state · immutable · ไม่ใช้ Redux/Zustand · Twist 6, 7 ผ่าน |
|  6  | **ฟอร์ม: react-hook-form + zod** |   15    | schema ไฟล์แยก · ครบ 4 ช่องตามกติกา · error ใต้ช่อง · `mode` เหมาะ · ขั้นต่ำ 3 ตัว · สถานะกำลังส่ง + การ์ดสรุป · Twist 8, 9 ผ่าน                                |
|  7  | **คุณภาพโค้ด**                   |    5    | `PokemonCard` ใช้ซ้ำจริง · `key` เสถียร · `npm run build` ผ่าน                                                                                                  |
|  8  | **README & การส่งงาน**           |   10    | README ครบ 3 หัวข้อ และข้อ 3 ตอบตรงประเด็น · ไม่มี `node_modules` ใน zip · แตก zip แล้วโครงไฟล์ถูกต้อง |
|     | **รวม**                          | **100** |                                                                                                                                                                 |

> 🔴 **สองอย่างที่ทำให้เสียคะแนนหนักที่สุด:** แตก zip แล้ว `npm install && npm run dev` รันไม่ขึ้น · ไม่ได้ใช้ React Router (สลับหน้าด้วย `useState`) — เช็กสองข้อนี้ก่อนส่งเสมอ

**คะแนนรายบุคคล:** **ทั้งสองคนได้คะแนนเท่ากันเสมอ** — Assignment นี้ไม่มีการปรับรายบุคคล

---

## 7. เช็กลิสต์ก่อนส่ง (ทำเองก่อน 09:00 น. เสาร์ 26 ก.ย. 69)

- [ ] `npm run build` ผ่าน ไม่มี error
- [ ] **แตก zip ที่จะส่งลงโฟลเดอร์ใหม่** → `npm install && npm run dev` รันขึ้นทันที (ทดสอบจริงก่อนส่ง)
- [ ] ไม่มี `node_modules` ใน zip · `package.json` อยู่ชั้นบนสุดของ zip
- [ ] ลองทำ Twist ข้อ 1–9 ด้วยตัวเองครบทุกข้อแล้ว
- [ ] `grep -rn "useContext(TeamContext)" src` เจอแค่บรรทัดเดียว (ใน `useTeam()`)
- [ ] `package.json` ไม่มี `redux` / `zustand`
- [ ] README ครบ 3 หัวข้อ ไม่มี `[ ... ]` ค้างอยู่
